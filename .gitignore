# Lazy.nvim lock file (optional - you might want to track this)
# lazy-lock.json

# Temporary files
*.tmp
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Plugin data/cache directories that shouldn't be tracked
.repro/
data/
state/
cache/

# Personal scratch files
scratch.*
notes.*

# Any local configuration that shouldn't be shared
local.lua